---
title: 单向数据同步：远程基准 + 本地状态缓存
order: 2
---

## 场景描述

以远程数据为唯一基准，只读取、不修改远程内容；远程提供资源列表等基础原始数据，本地仅独立存储用户私有状态（播放进度、收藏标记等）；

## 解决方案

1. 本地有资源表（方便查询）+本地属性表，通过资源 id 关联。
2. 每次进入页面，都获取远程的资源列表。
3. 保存资源的播放信息，放到本地属性表上。
4. 展示 = 远程数据+本地状态合并。

## 可能的问题

1. 本地存储越来越大。
   解决方案：只保留最近 100/200 条播放记录。
